#ifndef PROMOTEMOVE_H
#define PROMOTEMOVE_H

#include <string>
#include "move.h"
//#include "piece.h"
//#include "board.h"

class promoteMove : public Move {
public:
    // promoteMove(Board *b, Position start, Position end, std::string colour);
    // Board movePiece(std::string promoteTo) override;
};

#endif
