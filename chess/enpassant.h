#ifndef ENPASSANT_H
#define ENPASSANT_H

#include <string>
#include "move.h"
#include "piece.h"
#include "board.h"

class Enpassant: public Move {
    public:
    //Enpassant(Board *b, Position start, Position end, std::string colour);
    //Board movePiece(char moveTo = 0) override;
};

#endif
